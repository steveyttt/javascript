//function called "sum of cubes"
//function takes 2 parameters represented as a & b

function sumOfCubes2 (a, b) {
   // Do something
  return // return the output - can be used to exit a function
}

function sumOfCubes (a, b) {
  //var acubed = a*a*a;
  //var bcubed = b*b*b;
  //var sum = acubed + bcubed;
   return a*a*a + b*b*b;
 }

console.log(sumOfCubes(4, 9));

var fred = 99
console.log(sumOfCubes((1+2), fred))


function multiplyTrio (a, b, c) {
    return a * b * c
}

function maxOf2 (a, b) {
    if (a >= b) {
        return a
    }
    else {
        return b
    }
}